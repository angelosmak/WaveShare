<h1>List of all events</h1>

<% @events.each do |event| %>
  <%= link_to(event_path(event)) do %>
    <div>
      <h2><%= event.title %></h2>
      <h4><%= event.date %></h4>
      </div>
  <% end %>
<% end %>
