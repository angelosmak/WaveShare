<%= simple_form_for [@beach, @event] do |f| %>
  <%= f.input :title, required: true, placeholder: "Add an event Title"%>
  <%= f.input :date%>
  <%= f.input :description, placeholder: "Give a short description"%>
  <%= f.button :submit, 'Add', class: "btn btn-primary"%>
<% end %>
